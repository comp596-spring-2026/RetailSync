name: CI Production Gate

on:
  pull_request:
  push:
    branches: ["main", "develop", "release/**", "hotfix/**"]

concurrency:
  group: ci-production-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  NODE_VERSION: "20"
  PNPM_VERSION: "10.4.1"
  CI: "true"

jobs:
  quality:
    name: Typecheck + Lint
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      - name: Resolve pnpm store path
        id: pnpm-store
        run: echo "path=$(pnpm store path --silent)" >> "$GITHUB_OUTPUT"

      - name: Cache pnpm store
        uses: actions/cache@v4
        with:
          path: ${{ steps.pnpm-store.outputs.path }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --prefer-offline --fetch-retries=5 --fetch-retry-factor=2 --fetch-retry-mintimeout=20000 --fetch-retry-maxtimeout=120000

      - name: Typecheck
        run: pnpm typecheck

      - name: Lint
        run: pnpm lint

      - name: Summary
        run: |
          echo "## Quality Gate" >> $GITHUB_STEP_SUMMARY
          echo "- Typecheck: passed" >> $GITHUB_STEP_SUMMARY
          echo "- Lint: passed" >> $GITHUB_STEP_SUMMARY

  tests:
    name: Test Suite
    runs-on: ubuntu-latest
    timeout-minutes: 20
    needs: [quality]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      - name: Resolve pnpm store path
        id: pnpm-store
        run: echo "path=$(pnpm store path --silent)" >> "$GITHUB_OUTPUT"

      - name: Cache pnpm store
        uses: actions/cache@v4
        with:
          path: ${{ steps.pnpm-store.outputs.path }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Configure mongodb-memory-server cache directory
        run: |
          echo "MONGOMS_PREFER_GLOBAL_PATH=0" >> "$GITHUB_ENV"
          echo "MONGOMS_DOWNLOAD_DIR=$HOME/.cache/mongodb-binaries" >> "$GITHUB_ENV"

      - name: Cache MongoDB binaries
        uses: actions/cache@v4
        with:
          path: ~/.cache/mongodb-binaries
          key: ${{ runner.os }}-mongodb-binaries-${{ hashFiles('pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-mongodb-binaries-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --prefer-offline --fetch-retries=5 --fetch-retry-factor=2 --fetch-retry-mintimeout=20000 --fetch-retry-maxtimeout=120000

      - name: Prepare mongodb-memory-server cache
        run: |
          mkdir -p "$MONGOMS_DOWNLOAD_DIR"
          find "$MONGOMS_DOWNLOAD_DIR" -name '*.lock' -type f -delete || true

      - name: Run all tests
        run: pnpm test

      - name: Summary
        run: |
          echo "## Test Gate" >> $GITHUB_STEP_SUMMARY
          echo "- Unit and package tests: passed" >> $GITHUB_STEP_SUMMARY

  build:
    name: Build Artifacts
    runs-on: ubuntu-latest
    timeout-minutes: 20
    needs: [quality]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      - name: Resolve pnpm store path
        id: pnpm-store
        run: echo "path=$(pnpm store path --silent)" >> "$GITHUB_OUTPUT"

      - name: Cache pnpm store
        uses: actions/cache@v4
        with:
          path: ${{ steps.pnpm-store.outputs.path }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --prefer-offline --fetch-retries=5 --fetch-retry-factor=2 --fetch-retry-mintimeout=20000 --fetch-retry-maxtimeout=120000

      - name: Build all packages
        run: pnpm build

      - name: Upload client dist
        uses: actions/upload-artifact@v4
        with:
          name: client-dist
          path: client/dist
          if-no-files-found: error

      - name: Upload server dist
        uses: actions/upload-artifact@v4
        with:
          name: server-dist
          path: server/dist
          if-no-files-found: error

      - name: Summary
        run: |
          echo "## Build Gate" >> $GITHUB_STEP_SUMMARY
          echo "- Shared, server, and client builds: passed" >> $GITHUB_STEP_SUMMARY

  docker-smoke:
    name: Docker Smoke Check
    runs-on: ubuntu-latest
    timeout-minutes: 30
    needs: [build]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Docker compose build and startup
        id: compose-up
        continue-on-error: true
        run: docker compose up --build -d

      - name: Print compose service status
        if: ${{ always() }}
        run: docker compose ps

      - name: Print server logs after startup
        if: ${{ always() }}
        run: docker compose logs --no-color server

      - name: Fail if compose startup failed
        if: ${{ steps.compose-up.outcome != 'success' }}
        run: exit 1

      - name: Run smoke verification profile
        run: docker compose --profile verify run --rm smoke

      - name: Print compose logs on failure
        if: ${{ failure() }}
        run: docker compose logs --no-color

      - name: Shutdown compose stack
        if: ${{ always() }}
        run: docker compose down -v --remove-orphans

      - name: Summary
        run: |
          echo "## Docker Smoke Gate" >> $GITHUB_STEP_SUMMARY
          echo "- docker compose stack started successfully" >> $GITHUB_STEP_SUMMARY
          echo "- smoke profile checks passed" >> $GITHUB_STEP_SUMMARY

  final-status:
    name: Final CI Status
    runs-on: ubuntu-latest
    needs: [quality, tests, build, docker-smoke]
    if: ${{ always() }}

    steps:
      - name: Evaluate dependency jobs
        run: |
          quality="${{ needs.quality.result }}"
          tests="${{ needs.tests.result }}"
          build="${{ needs.build.result }}"
          docker_smoke="${{ needs.docker-smoke.result }}"

          echo "quality => $quality"
          echo "tests => $tests"
          echo "build => $build"
          echo "docker-smoke => $docker_smoke"
          echo "## Final Status" >> $GITHUB_STEP_SUMMARY
          if [ "$quality" = "success" ] && [ "$tests" = "success" ] && [ "$build" = "success" ] && [ "$docker_smoke" = "success" ]; then
            echo "- All CI quality gates passed." >> $GITHUB_STEP_SUMMARY
            exit 0
          else
            echo "- One or more CI quality gates failed." >> $GITHUB_STEP_SUMMARY
            exit 1
          fi
